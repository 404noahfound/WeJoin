extends ../layout

block content
	h1= title
	p Welcome to #{title}.
	p Are you sure to delete:
		each note in notes
			p #{note.title}
			p #{note.content}
	form(name="note", method="post")
		div.actions
			input(type="submit", value="Yes")